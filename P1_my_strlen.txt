//comment
/* 1. C++ program  to Create Custom function for strlen, strcpy, strchr, strstr  eg: create functions mystrlen, mystrcpy which behaves in  
     same manner as these library functions*/
										
													// my_strlen()
#include <iostream>
using namespace std;
int my_strlen(char *s);

int main()
{
     char str[50];
     cout<<"Enter your string: "<<endl;
     cin>>str;
    cout<<"Length of string is: "<<my_strlen(str)<<endl;
}

int my_strlen(char *s )
{
    int count;
    for(count=0;s[count]!='\0';count++);
    return count;
}
